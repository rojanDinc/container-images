# Container Images

A collection of custom container images for development and deployment workflows.

## Overview

This repository contains custom Dockerfile definitions for applications where the default base images need customization or enhancement. Images are automatically built and published to a container registry via GitHub Actions.

